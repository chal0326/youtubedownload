{
  "name": "youtubedownload",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bundle": "esbuild downloader.js --bundle --platform=node --target=node18 --external:fluent-ffmpeg --external:ffmpeg-static --outfile=dist/downloader.js",
    "build": "npm run bundle && pkg -t node18-macos,node18-win .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ffmpeg-static": "^4.4.0",
    "fluent-ffmpeg": "^2.1.2",
    "fs": "^0.0.1-security",
    "youtubei.js": "^17.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "pkg": "^5.8.1"
  },
  "bin": "dist/downloader.js"
}
